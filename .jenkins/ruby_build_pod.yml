---
apiVersion: v1
kind: Pod
metadata:
  labels:
  jenkins/exception-handling: 'true'
  namespace: jenkins
  name: exception-handling
spec:
  containers:
  - name: ruby
    image: ruby:2.6.5
    tty: true
    resources:
    requests:
      memory: "100Mi"
    command:
      - cat
